/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['images.unsplash.com', 'asset.kompas.com','example.com' ],
  },
};

export default nextConfig;
