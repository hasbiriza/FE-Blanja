/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['images.unsplash.com','via.placeholder.com', 'asset.kompas.com','example.com','res.cloudinary.com' ],
  },
};

export default nextConfig;
